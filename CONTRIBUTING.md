# 기여 안내

DevDemangle에 기여해 주셔서 감사합니다.

**가장 쓸모 있는 기여는 코드가 아니라 실측 제보입니다.** 아래 [용어·별칭 제보](#1-용어별칭-제보--가장-환영합니다)를
먼저 봐 주세요.

---

## 1. 용어·별칭 제보 — 가장 환영합니다

이 라이브러리의 성능은 코드보다 **용어집**이 좌우합니다. 그리고 별칭은 규칙으로 예측할 수
없습니다 — `GitHub`이 `"기터벳"`으로 깨질지 `"기터브"`로 깨질지는 실제로 돌려봐야 압니다.

그래서 **"내 환경에서 STT가 이 용어를 이렇게 뱉었다"**는 제보가 직접적인 개선이 됩니다.

이슈에 이렇게 적어 주세요. **아래는 형식을 보이기 위한 예시입니다.**

```
용어      GitHub
깨진 형태  기터벳
문장      "기터벳의 리포지토리 만들었어?"
STT       faster-whisper large-v3 / float16 / CUDA
```

**추측한 형태가 아니라 실제로 나온 것**이면 됩니다. 문장 전체가 있으면 조사·어미가 붙은
형태까지 볼 수 있어 더 좋습니다.

### 용어집을 직접 고치려면

`data/terms.yaml`을 편집하고 PR을 열어 주세요.

```yaml
- canonical: GitHub
  aliases: [깃허브, 깃헙, 기터벳]
  translations: { }        # 고정 번역어가 필요할 때만
```

> ⚠️ **별칭을 늘리면 재현율은 오르지만 오탐도 같이 늡니다.** 예를 들어 `"파일선"`(Python)을
> 넣으면 일상어 `"파일은"`의 유사도가 0.67에서 0.77로 올라갑니다. 임계값이 0.78이라 아슬합니다.
>
> **2음절 이하이거나 일상어와 겹치는 별칭은 신중하게 봐 주세요.** `"제이슨"`(사람 이름),
> `"시퀄"`(속편), `"파이선"`(뱀)이 그런 예입니다. 이미 등록돼 있지만 문맥으로 가릴 방법이 없습니다.

---

## 2. 개발 환경

Python 3.13 이상, [uv](https://docs.astral.sh/uv/)가 필요합니다.

```bash
git clone https://github.com/4thIS/opensource_contest_devdemangle.git
cd opensource_contest_devdemangle
uv sync --extra dev
uv run pytest
```

GPU가 없어도 **코어 테스트는 전부 돕니다.** 음성 파일이 필요한 테스트는 자동으로 건너뜁니다.

```bash
uv run --extra cuda --extra dev pytest     # GPU 테스트까지
```

---

## 3. 코드 기여

### 지켜야 할 설계 원칙 하나

**코어는 오디오도 번역도 모릅니다.**

```python
import devdemangle          # faster_whisper·torch·transformers가 로드되면 안 됨
```

`glossary`·`detect`·`fuzzy`·`correct`에서 무거운 라이브러리를 import하면 이 원칙이 깨집니다.
오디오는 `devdemangle.stt`, 번역은 `devdemangle.translate` 서브패키지에 둡니다.

이 분리 덕분에 코어를 번역 없이 떼어 쓸 수 있고, 테스트가 음성 파일 없이 문자열만으로 돕니다.

### 탐지 규칙을 건드릴 때

경계 규칙은 **오탐과 맞바꾸는 판단**입니다. 숫자 없이 고치지 말아 주세요.

- 임계값·길이 상한 같은 상수를 바꾸면 **무엇을 얻고 무엇을 잃는지 같이 적어 주세요**
- 특히 "실측에서 오탐이 안 나왔다"만으로는 부족합니다 — 개발 대화 코퍼스에는 일상어가
  거의 없어서, 오탐이 없는 게 아니라 오탐이 날 문장이 없는 경우가 많습니다

### 테스트

기능과 버그 수정에는 테스트를 붙여 주세요. 실패하는 테스트를 먼저 쓰고, **실패 이유가
의도한 것인지 확인한 뒤** 통과시키는 순서를 권합니다.

알려진 한계는 지우지 말고 `xfail(strict=True)`로 고정합니다. 나중에 누가 고치면
XPASS로 알려 줍니다.

```python
@pytest.mark.xfail(strict=True, reason="정답과 오탐이 0.800에서 겹친다")
def test_known_limit_...():
    ...
```

---

## 4. 커밋과 PR

커밋 메시지는 `<타입>: <한 줄 요약>` 형식입니다.

```
feat:     기능 추가
fix:      버그 수정
docs:     문서
test:     테스트
refactor: 동작 변경 없는 정리
chore:    빌드·설정
```

**무엇을 했는지보다 왜 했는지를 적어 주세요.** 무엇은 diff에 있습니다.

PR에는 다음이 있으면 좋습니다.

- 무엇을 바꿨고 **왜** 그렇게 했는지
- 탐지 규칙을 건드렸다면 **측정 결과** (얻은 것 / 잃은 것)
- 알려진 한계가 생겼다면 그것도 함께

---

## 5. 커밋하지 말아 주세요

```
음성 파일 (*.wav, *.m4a, *.mp3)     개인정보이고 되돌릴 수 없습니다
모델 가중치                          라이선스가 배포물 전체에 얽힙니다
개인 경로·이메일·실명
API 키·토큰
```

`git add .` 대신 파일을 지정해서 스테이징하고, 커밋 전에 `git status`로 딸려온 것이 없는지
확인해 주세요.

---

## 6. 라이선스

기여하신 코드는 이 저장소와 같은 [MIT License](LICENSE)로 배포됩니다.
