# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따른다.

---

## [1.0-submission] — 2026-08-26

2026 오픈소스 개발자대회 출품 시점의 상태.

### 코어

- **용어집** — YAML에서 표준형·별칭·고정 번역어를 읽는다. 로딩 시 중복·충돌을 검증하고,
  위반은 첫 건에서 멈추지 않고 전부 모아 한 번에 보고한다.
- **등록 탐지** — Aho-Corasick으로 표준형과 별칭을 찾는다. 대소문자를 무시하고,
  뒤에 붙은 조사는 매치 구간에서 뺀다(`"도커를"` → `Docker` + `를`).
- **식별자 탐지** — 명령행 플래그·camelCase·snake_case를 모양으로 찾는다.
  표준형을 모르므로 **보정하지 않고 보호만 한다.**
- **소리 탐지** — 한글을 로마자로 정규화해 등록 별칭과 편집거리를 잰다.
  용어집에 없는 발음 변형(`"기터브"` → `GitHub`)을 잡는다.
- **보정** — 위 셋을 합쳐 겹침을 해소하고 표준형으로 치환한다.
  결과 좌표(`Span`)는 보정된 텍스트 기준으로 다시 센다.

### STT

- faster-whisper 어댑터. 한국어 고정, VAD로 무음 제거, 용어집을 `hotwords`로 전달.
- Windows에서 CUDA DLL을 PATH에 먼저 등록한 뒤 라이브러리를 import한다.
- `hotwords`를 끄고 전사할 수 있다. 보정 단계가 혼자 무엇을 하는지 재려면 필요하다.

### 번역

- **용어를 가린 뒤 번역한다.** 찾은 용어를 플레이스홀더로 바꿔 번역기에 넘기고,
  나온 문장에서 되돌린다. 플레이스홀더는 대문자 글자만 쓴다 — 따옴표나 밑줄을 덧붙이면
  번역기가 통째로 먹는 것이 실측으로 확인됐다.
- 번역이 플레이스홀더를 삼켜 되돌리지 못한 용어는 `lost`로 보고한다.
- 용어집에 고정 번역어가 있으면 그것을 쓰고, 없으면 원문을 유지한다.

### 데모 앱

- Gradio 단일 화면. 마이크 입력 또는 파일 업로드를 받아 전사·보정·번역을 한 번에 보여준다.
- 찾은 용어를 **탐지 방법별로 다른 색**으로 칠한다. 무엇을 어떻게 찾았는지가 화면에 남는다.
- `gradio`는 `main()` 안에서만 import한다. 코어를 떼어 쓰는 사람에게 UI 라이브러리를 지우지 않는다.

### 평가

- `eval/benchmark.py` — 단계별 비교표와 픽스처 채점을 산출한다.
- 실측 전사를 저장소에 포함해 **GPU 없이도 수치가 재현된다.**
- 측정할 때마다 실행일·커밋·임계값·용어집 크기와 함께 `eval/results/`에 남긴다.

### 설계

- **코어는 오디오도 번역도 모른다.** `import devdemangle`이 `faster_whisper`·`torch`·
  `transformers`를 불러오지 않는다. 오디오는 `devdemangle.stt` 서브패키지에 격리했다.
- **모델 가중치를 저장소에 넣지 않는다.** 실행 시 다운로드한다.

### 측정

음성 코퍼스(3인 녹음) 기준. `uv run python eval/benchmark.py`로 재현된다.

| 표본 | 문장 | STT만 | + hotwords | + 보정 (hotwords 없이) |
|---|---|---|---|---|
| 전체 | 64 | 14/69 = 20.3% | 45/69 = 65.2% | **63/69 = 91.3%** |
| 1차 녹음 | 21 | 8/23 = 34.8% | 17/23 = 73.9% | 22/23 = 95.7% |
| 문장 끝 | 16 | 2/18 = 11.1% | 11/18 = 61.1% | 14/18 = 77.8% |
| 용어집 확장 | 27 | 4/28 = 14.3% | 17/28 = 60.7% | 27/28 = 96.4% |

2·3차 녹음은 용어가 문장 끝에 오거나 신규 용어를 쓰는 어려운 표본이라 1차보다 낮다.
`large-v3` / `float16` / CUDA 기준이며, **조건이 다른 수치를 같은 표에 올리지 않는다.**

### 알려진 한계

- 앞에 다른 글자가 붙으면 안 잡는다 (`"우리깃허브"`). 띄어 쓴 `"우리 깃허브"`는 잡는다.
- 어미는 떼지 않는다 (`"깃허브다"`, `"파이썬입니다"`). 문장 끝 실측 16건 중 3건이 여기서 죽는다.
- 짧은 음차는 소리로 찾지 않는다 (`Vue` ← `"뷰"`).
- 한 어절씩만 본다. `"패스트 API"`처럼 용어 중간에 공백이 들어가면 놓친다.
- 동음이의어를 문맥으로 가리지 못한다 (`"파이선"`(뱀) ↔ `Python`).

### 아직 없는 것

- **오탐 픽스처 기반 정밀도·오탐률 측정.** 하니스는 있고 픽스처는 검수 중이다.
  위 측정 표에 오탐률이 없는 것은 그래서다 — 64문장이 전부 용어가 든 문장이라
  오탐을 잴 표본이 아니다.
- 실시간 연속 스트리밍. 지금은 발화 단위로 끊어 처리한다.
- 영→한 번역. 용어집 스키마는 양방향을 이미 지원한다.

---

## 표기에 관하여

이 프로젝트는 **버전을 올리기보다 태그로 시점을 남긴다.** 대회 2차 평가가 10월이라
"제출한 것이 정확히 무엇이었나"를 알아야 하기 때문이다.

```
v0.1-risk-verified   1주차 리스크 검증 완료
v1.0-submission      2026-08-26 제출본
```
